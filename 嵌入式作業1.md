# 安裝虛擬機

作者: 許禕恩
系級: 資管碩一
環境: Ubuntu (VirtualBox)  
實驗目的: arm-linux-gnueabihf  
工具版本: Binutils 2.40  

---

## 1. 實驗目的

本實驗的目的是在 x86 Ubuntu 系統 上建立 ARM 交叉編譯工具鏈（Cross Compiler Toolchain）。

所謂 交叉編譯（Cross Compilation） 指的是：

在一種 CPU 架構的電腦上，編譯出另一種 CPU 架構可以執行的程式。

本實驗完成後，可以在 Ubuntu 上產生 ARM 程式，例如：
```bash
arm-linux-gnueabihf-as
arm-linux-gnueabihf-ld
arm-linux-gnueabihf-objdump
```

## 2. 建立工作目錄
```bash
mkdir -p ~/WORK
cd ~/WORK
```
`mkdir -p`：建立資料夾

`~/WORK`：位於使用者 home 目錄

`cd`：進入該資料夾

此資料夾將用來存放：

原始碼

編譯目錄

安裝完成的 toolchain

## 3. 安裝套件
```bash
sudo apt update
sudo apt install -y build-essential wget git texinfo
```
## 4. 下載 Binutils 原始碼
`wget http://ftp.gnu.org/gnu/binutils/binutils-2.40.tar.gz`

## 5. 解壓縮原始碼
`tar -zxvf binutils-2.40.tar.gz`

## 6. 建立編譯目錄
```bash
mkdir build_binutils
cd build_binutils
```
## 7. 設定configure
```bash
../binutils-2.40/configure \
--prefix=$HOME/WORK/crossgcc1 \
--target=arm-linux-gnueabihf \
--disable-nls \
--disable-werror
```
## 8. 開始編譯並安裝 Binutils
```bash
make -j$(nproc)
make install
```
## 9. 新增捷徑
```bash
export PATH=$HOME/WORK/crossgcc2/bin:$PATH
alias armgcc='arm-linux-gnueabihf-gcc'
```
# 實驗報告 Q&A

## 一、請說明參數 `--with-sysroot`、`--with-arch` 的目的

在建立 cross compiler 的過程中，`--with-sysroot` 和 `--with-arch` 是兩個非常重要的參數，分別用來設定「系統環境」與「CPU架構」。

首先，`--with-sysroot` 是用來指定目標系統的根目錄（root filesystem）。也就是說，當我們在主機（例如 Ubuntu）上進行編譯時，編譯器並不是使用本機的函式庫與標頭檔，而是去讀取這個 sysroot 目錄裡的內容，例如 `/usr/include`、`/usr/lib` 等。這樣可以確保編譯出來的程式，是符合目標裝置（例如 Raspberry Pi）的執行環境。因此可以把它理解成：**讓編譯器模擬在目標裝置上編譯程式**。

接著，`--with-arch` 則是用來指定目標 CPU 的架構。不同的 CPU 架構（例如 armv7、armv8）會對應到不同的機器指令，因此這個參數會直接影響編譯出來的程式是否能在目標裝置上正常執行。簡單來說，這個參數是在告訴編譯器：「你要幫我產生哪一種 CPU 可以跑的程式」。

---

## 二、Raspberry Pi 3B+ 是否可以設為 `--with-arch=armv8`

Raspberry Pi 3B+ 使用的是 ARM Cortex-A53 處理器，這顆 CPU 本身是支援 ARMv8 架構的，因此從硬體角度來看，是可以使用 `--with-arch=armv8` 的。

不過，是否能這樣設定，還需要考慮作業系統的位元數。如果 Raspberry Pi 上安裝的是 64-bit 作業系統（例如 Ubuntu 64-bit 或 aarch64 系統），那麼設定 `--with-arch=armv8` 是沒有問題的，甚至可以獲得較好的效能。

但如果使用的是 Raspberry Pi OS（預設為 32-bit），那麼就不建議設定為 `armv8`。因為這樣會產生 64-bit 的執行檔，而 32-bit 系統是無法執行 64-bit 程式的，最終會導致程式無法運作。

---

## 三、結論

綜合以上說明，可以得到以下結論：

- `--with-sysroot`：用來指定目標系統的函式庫與標頭檔位置，確保編譯環境正確  
- `--with-arch`：用來指定目標 CPU 架構，決定程式能在哪種硬體上執行  

而在 Raspberry Pi 3B+ 的情境中：

- 若為 32-bit 系統 → 建議使用 `armv7-a`  
- 若為 64-bit 系統 → 可以使用 `armv8`  

簡單來說，**最終要不要使用 armv8，關鍵不在硬體，而是在作業系統是否為 64-bit。**
