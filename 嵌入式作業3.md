# 🧪 Raspberry Pi 3 最簡易 Linux 系統實驗報告

---

## 一、實驗目的

本實驗透過 Buildroot 建立一個最簡單的 Linux 系統，並在 Raspberry Pi 3 上成功開機。
主要學習內容包含：

* Bootloader、Kernel、Root filesystem 的角色
* Embedded Linux 系統建構流程
* SD 卡燒錄與開機流程

---

## 二、實驗環境

### 軟體

* Ubuntu（VirtualBox）
* Buildroot
* balenaEtcher（燒錄工具）

### 硬體

* Raspberry Pi 3 Model B
* microSD 卡（32GB）
* HDMI、鍵盤、電源

---

## 三、系統架構

```id="h8r3zt"
Bootloader → Kernel → Root filesystem
```

---

## 四、實驗流程

---

### Step 1：下載 Buildroot

```bash id="m8u0b2"
git clone https://gitlab.com/buildroot.org/buildroot.git
cd buildroot
```

---

### Step 2：設定 Raspberry Pi 3

```bash id="v4r6z3"
make raspberrypi3_64_defconfig
```

---

### Step 3：設定 Root filesystem

```bash id="7y9h3b"
make menuconfig
```

設定：

```id="c7r2dj"
Filesystem images → tar the root filesystem → bzip2
```

---

### Step 4：編譯系統

```bash id="1kz1v5"
make -j4
```

👉 產生：

* Kernel（Image）
* rootfs.tar.bz2
* sdcard.img

---

### Step 5：燒錄 SD 卡

使用 balenaEtcher：

1. 選擇 sdcard.img
2. 選擇 SD 卡（約 32GB）
3. 進行燒錄

---

### Step 6：開機測試

1. 插入 SD 卡至 Raspberry Pi
2. 接上電源
3. 開機後輸入：

```id="q6c9ep"
root
```

---

## 五、系統驗證

---

### 1️⃣ 檢查 root filesystem

```bash id="t4xtl7"
ls /
```

結果：

```id="2rrkbx"
bin dev etc proc sys tmp usr var
```

---

### 2️⃣ 檢查 kernel

```bash id="ymzzl1"
uname -a
```

---

### 3️⃣ 檢查 CPU

```bash id="h4u1ql"
cat /proc/cpuinfo
```

---

### 4️⃣ BusyBox 測試

```bash id="d0j30f"
ls /bin
```

👉 顯示基本指令（ls, cp, mv）

---

## 六、實驗中遇到的超大問題與解決方法

---

### ❗ 問題 1：VirtualBox 無法偵測 USB（SD卡）

#### 🔍 問題描述

在 Ubuntu（VirtualBox）中執行：

```bash id="t8klcd"
lsblk
```

僅顯示系統磁碟（sda），未出現 SD 卡。

---

#### 🧠 問題原因

* VirtualBox 預設不會自動掛載 USB 裝置
* SD 卡透過讀卡機連接，但未被 VM 接管
* 缺少 USB 權限或 Extension Pack 支援

---

#### 🔧 嘗試過的解法

1. 在 VirtualBox 中設定 USB controller
2. 安裝 Extension Pack
3. 嘗試 Devices → USB 掛載裝置
4. 嘗試使用 sudo 設定權限（未採用）

---

#### ❌ 最終問題

仍無法在 VM 中成功辨識 SD 卡

---

### ✅ 最終解決方式（關鍵🔥）

👉 放棄 VirtualBox USB

改用：

```text id="f0vl9k"
Windows + balenaEtcher
```

直接燒錄 SD 卡

---

#### 🧠 原因分析

* VirtualBox USB passthrough 不穩定
* 權限設定複雜
* 使用 GUI 工具更簡單可靠

---

### ❗ 問題 2：無法拖曳檔案（Drag & Drop）

#### 🔍 問題描述

顯示：

```text id="q3h9it"
Dragging from guest to host not supported
```

---

#### 🧠 原因

* 未正確安裝 Guest Additions

---

#### 🔧 解決方式

改用：

👉 網路上傳（WeTransfer）
👉 或其他檔案傳輸方式

---

## 七、Q&A（實驗問題）

---

### Q1：firmware 檔案用途

| 檔案           | 功能                     |
| ------------ | ---------------------- |
| bootcode.bin | 第二階段 bootloader        |
| start.elf    | GPU firmware，載入 kernel |
| fixup.dat    | 記憶體配置                  |
| config.txt   | 系統設定                   |
| cmdline.txt  | kernel 啟動參數            |

---

### Q2：cmdline.txt 說明

```id="l6db51"
console=serial0,115200 root=/dev/mmcblk0p2 rootfstype=ext4
```

---

### Q3：Buildroot 特性

* 無 apt
* 系統精簡
* 開機快速
* 適用嵌入式系統

---

## 八、實驗結果

成功完成：

* Linux 系統建構
* Kernel 編譯
* SD 卡燒錄
* Raspberry Pi 開機

---

## 九、心得

本實驗讓我了解嵌入式 Linux 系統的完整建構流程，並實際操作從編譯到開機的每個步驟。
在過程中遇到 VirtualBox USB 無法使用的問題，也學習到選擇適當工具的重要性。

---

## 十、結論

成功建構並執行最簡 Linux 系統，並理解：

* Bootloader、Kernel、Rootfs 關係
* 系統開機流程
* Embedded Linux 基本概念

---
