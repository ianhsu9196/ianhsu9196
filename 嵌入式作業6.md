# System Backup and Configuration 實驗報告

Raspberry Pi UART 登入、Image 掛載與系統備份觀念整理

| 項目 | 內容 |
| --- | --- |
| 姓名 | 許禕恩 |
| 學號 | 614530038 |
| 系級 | 資管碩一 |
| 實驗平台 | Raspberry Pi 3 Model B |
| 工具 | Ubuntu VM、PuTTY、USB to TTL CH340、Buildroot |
| 通訊設定 | 115200 8N1 |

## 一、實驗目的

本實驗主要是練習 Embedded Linux 系統的備份與設定檢查流程。我透過 UART 連線登入 Raspberry Pi，確認 Buildroot 系統可以正常啟動，再回到 Ubuntu 虛擬機中掛載 boot 與 root filesystem 檔案，觀察系統內部內容。

透過這個過程，可以理解 `dd` 與 `rsync` 在系統備份上的差異，也能熟悉 image file、filesystem 與 loop device 的概念。

## 二、實驗環境

| 項目 | 內容 |
| --- | --- |
| 硬體設備 | Raspberry Pi 3 Model B、microSD card、USB to TTL 模組 |
| 連線工具 | PuTTY，Serial baud rate 設為 115200 |
| 轉接晶片 | 原先 PL2303HXA 無法正常使用，後來改用 CH340 |
| 作業環境 | Ubuntu VirtualBox、Buildroot 產生的 `boot.vfat` 與 `rootfs.ext4` |
| 相關指令 | `mount`、`umount`、`fdisk`、`losetup`、`dd`、`rsync`、`sync` |

## 三、實驗步驟

### 1. UART 接線與 PuTTY 設定

- 先將 USB to TTL 與 Raspberry Pi 連接，GND 接 GND，TX 與 RX 要交叉接，也就是 TX 接 RX、RX 接 TX。
- 在 Windows 裝置管理員確認 COM Port，接著打開 PuTTY，選擇 Serial 模式，baud rate 設成 115200。
- 開機後若畫面正常輸出 Buildroot 訊息，就可以用 root 登入。

### 2. 確認 Raspberry Pi 可以正常開機

- 成功登入後，先確認終端機可以操作，代表 bootloader、kernel 與 root filesystem 已經可以正常啟動。
- 這一步的重點不是修改系統，而是確認 UART 連線與開機流程是否正常。

### 3. 回到 Ubuntu VM 分析 image file

- 因為 mount 與 image 分析指令應該在 Ubuntu 環境執行，不是在 Raspberry Pi 的 Buildroot shell 執行，所以確認登入成功後我切回 Ubuntu VM 操作。
- 原本嘗試直接掛載 `sdcard.img`，但 partition table 解析不順利，因此改用 Buildroot 產生的 `boot.vfat` 與 `rootfs.ext4` 進行掛載。

### 4. 掛載 boot 與 root filesystem

- 建立掛載資料夾後，使用 `mount` 指令分別掛載 `boot.vfat` 與 `rootfs.ext4`。
- 掛載完成後，進入目錄查看 `config.txt`、kernel image、`/bin`、`/etc`、`/lib` 等內容，確認系統檔案可以被正常讀取。

### 5. 備份觀念整理

- `dd` 是 bit-by-bit 複製，適合完整複製整顆磁碟或整個 image，但檔案通常較大、速度也可能比較慢。
- `rsync` 是檔案層級同步，只複製變動的內容，比較適合日常備份或更新 root filesystem。

## 四、實作指令整理

以下指令是本實驗會用到或觀念上需要理解的操作，實際裝置名稱需依照當下環境調整。

```bash
# 查看磁碟與分割區
lsblk
sudo fdisk -l

# 建立掛載點
sudo mkdir -p /mnt/boot /mnt/rootfs

# 掛載 Buildroot 產生的 filesystem image
sudo mount boot.vfat /mnt/boot
sudo mount rootfs.ext4 /mnt/rootfs

# 查看掛載後的內容
ls /mnt/boot
ls /mnt/rootfs

# 完成後解除掛載
sudo umount /mnt/boot
sudo umount /mnt/rootfs
```

## 五、實驗結果

本次實驗成功透過 UART 登入 Raspberry Pi 的 Buildroot 系統，並在 Ubuntu VM 中掛載 `boot.vfat` 與 `rootfs.ext4` 來查看系統內容。掛載後可以看到 boot 相關檔案與 root filesystem 內的目錄結構，代表即使不重新燒錄 SD 卡，也可以透過 image file 分析系統內容。

整體結果顯示，我已理解 Raspberry Pi 開機時 boot partition 與 root filesystem 的角色，也能分辨 `dd` 與 `rsync` 在備份方式上的差異。

## 六、問題與解決方式

| 問題 | 原因 | 解決方式 |
| --- | --- | --- |
| USB to TTL 無法使用 | 一開始使用 PL2303HXA，但 Windows 端驅動不支援或被封鎖，導致 PuTTY 無法正常連線。 | 改用 CH340 模組後即可正常辨識 COM Port。 |
| PuTTY 開啟後沒有畫面 | 主要原因是 TX/RX 接線方向不對。 | 修正成 TX 接 RX、RX 接 TX，並確認 GND 有共地後，畫面就可以正常顯示。 |
| 指令下在錯誤環境 | 一開始把 mount、image 分析指令下在 Raspberry Pi 的 Buildroot shell。 | 改回 Ubuntu VM 執行 image 掛載與檔案分析。 |
| `sdcard.img` 無法順利 mount | 嘗試 `fdisk`、`losetup`、`kpartx` 時 partition table 沒有正常解析。 | 改用 `boot.vfat` 與 `rootfs.ext4` 直接掛載。 |
| 虛擬機讀不到 SD card | VirtualBox 對 USB passthrough 支援不穩定。 | 改以 image file 方式操作，避免被實體 SD 卡辨識問題卡住。 |

## 七、觀念整理

**UART**：一種序列通訊方式，本實驗用來從電腦端登入 Raspberry Pi，方便觀察開機訊息與操作系統。

**loop device**：把一般檔案模擬成磁碟裝置，常用在掛載 image file 或分析磁碟映像檔。

**dd**：以區塊或位元方式複製資料，可以完整複製磁碟，但不會判斷檔案是否有變動。

**rsync**：檔案同步工具，可以只同步差異部分，適合做增量備份。

**boot.vfat**：放置 Raspberry Pi 開機需要的 bootloader、kernel、dtb 與 config 等檔案。

**rootfs.ext4**：Linux 系統的 root filesystem，包含 `/bin`、`/etc`、`/lib` 等系統執行所需目錄。

## 八、結論

本次實驗完成了 UART 登入、image file 掛載與系統備份觀念整理。過程中雖然遇到 USB to TTL 驅動、TX/RX 接線、mount 環境判斷與 `sdcard.img` 掛載失敗等問題，但最後透過更換 CH340、修正接線、切回 Ubuntu VM，以及改用 `boot.vfat` 與 `rootfs.ext4` 掛載的方式完成實驗。

透過本次實作，我更清楚 Embedded Linux 系統中 boot partition、root filesystem、image file 與備份工具的關係，也理解 `dd` 適合完整備份，而 `rsync` 適合檔案同步與增量備份。
