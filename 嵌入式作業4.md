# 🧪 SD Card Partition 實驗報告

作者：許禕恩
系級：資管碩一

---

# 一、實驗目的（Objective）

本實驗的目的是學習如何手動建立 Raspberry Pi 可開機之 SD 卡，並理解 Embedded Linux 系統的啟動流程。透過 fdisk 分割 SD 卡、建立檔案系統（filesystem）、以及燒錄 root filesystem，使 Raspberry Pi 能成功開機。

主要學習內容包含：

* SD 卡 partition 設計（FAT32 + ext4）
* Linux 檔案系統建立（mkfs）
* Boot 與 Root filesystem 的關係
* Raspberry Pi 開機流程（Bootloader → Kernel → RootFS）

---

# 二、實驗環境（Environment）

## 軟體環境

* Ubuntu（VirtualBox）
* Buildroot
* balenaEtcher（Windows 燒錄工具）

## 硬體環境

* Raspberry Pi 3 Model B
* microSD 卡（≥ 4GB）
* 電源、HDMI、鍵盤

---

# 三、系統架構（System Architecture）

Raspberry Pi SD 卡結構如下：

```
Partition 1 (mmcblk0p1) → FAT32 → Boot Partition
Partition 2 (mmcblk0p2) → ext4 → Root Filesystem
```

開機流程：

```
Boot ROM → bootcode.bin → start.elf → Kernel → RootFS
```

---

# 四、實驗流程（Procedure）

## Step 1：建立虛擬 SD 卡（避免 VM 無法讀取 USB）

```bash
dd if=/dev/zero of=sdcard.img bs=1M count=4096
```

---

## Step 2：使用 loop device 模擬 SD 卡

```bash
losetup -Pf sdcard.img
```

---

## Step 3：使用 fdisk 分割磁碟

```bash
fdisk /dev/loop20
```

### 建立 Partition

* Partition 1：

  * 大小：64MB
  * 類型：FAT32
  * 設為 bootable

* Partition 2：

  * 剩餘空間
  * 類型：Linux (ext4)

---

## Step 4：建立檔案系統

```bash
mkfs.vfat -n BOOT /dev/loop20p1
mkfs.ext4 -L filesystem /dev/loop20p2
```

---

## Step 5：掛載 BOOT 分區

```bash
mkdir /mnt/boot
mount /dev/loop20p1 /mnt/boot
```

---

## Step 6：複製開機檔案

```bash
cp *.dtb /mnt/boot
cp Image /mnt/boot
cp -rf rpi-firmware/* /mnt/boot
```

---

## Step 7：寫入 Root Filesystem

```bash
dd if=rootfs.ext2 of=/dev/loop20p2
```

---

## Step 8：燒錄至實體 SD 卡

使用 balenaEtcher 將 `sdcard.img` 寫入 SD 卡。

---

# 五、實驗結果（Result）

成功將 SD 卡插入 Raspberry Pi 並開機，系統正常啟動。

驗證指令：

```bash
cat /proc/partitions
```

顯示：

```
mmcblk0
mmcblk0p1
mmcblk0p2
```

```bash
cat /proc/cmdline
```

顯示：

```
root=/dev/mmcblk0p2
```

代表 root filesystem 正確掛載。

---

# 六、問題與解決（Problems & Solutions）

## 問題 1：VirtualBox 無法讀取 SD 卡

原因：

* VM 無法穩定辨識 USB 裝置

解決：

* 使用 loop device 模擬 SD 卡
* 最後再輸出 image 至實體 SD 卡

---

## 問題 2：loop device 不足

錯誤訊息：

```
cannot find an unused loop device
```

解決：

* 使用：

```bash
losetup -Pf sdcard.img
```

* 或手動建立 loop device

---

## 問題 3：fdisk 分割錯誤

錯誤：

* 未在正確時機輸入 `+64M`

解決：

* 必須在 "Last sector" 時輸入

---

## 問題 4：partition 未顯示

原因：

* kernel 未 reload partition table

解決：

```bash
losetup -d /dev/loop20
losetup -Pf sdcard.img
```

---

## 問題 5：Windows 顯示需格式化

原因：

* Windows 無法辨識 ext4

解決：

* 忽略，不可格式化

---

# 七、問題回答（Q&A）

## 題目：

### 解釋以下指令：

```bash
mkfs.ext4 -L filesystem /dev/sdd2
```

---

### 解釋：

* `mkfs.ext4`

  * 建立 ext4 檔案系統
  * 將 partition 格式化為 Linux 可用格式

* `-L filesystem`

  * 設定檔案系統標籤（Label）
  * 名稱為 "filesystem"

* `/dev/sdd2`

  * 指定要格式化的裝置（第二個 partition）

---

# 八、討論（Discussion）

本實驗透過手動方式建立 Raspberry Pi 開機 SD 卡，深入理解 embedded Linux 系統架構。相較於直接使用現成映像檔，此方法讓我們了解 bootloader、kernel 與 root filesystem 的關係。

此外，透過 loop device 的使用，成功解決虛擬機無法讀取 USB 的問題，並提升對 Linux 裝置管理的理解。

---

# 九、結論（Conclusion）

本實驗成功完成：

* SD 卡 partition 設計
* Linux filesystem 建立
* Raspberry Pi 開機流程理解
* 實際開機驗證

證明所建立之 SD 卡可正常啟動 Linux 系統，達成本實驗目標。

---
