🧪 System Backup and Configuration 實驗報告

作者：許禕恩
系級：資管碩一

一、實驗目的（Objective）

本實驗旨在學習 Embedded Linux 系統操作，主要包含以下三個重點：

透過序列埠（UART）登入 Raspberry Pi
在不燒錄 SD 卡的情況下，從 image file 擷取系統檔案
了解系統備份方法（dd 與 rsync）的原理
二、實驗環境（Environment）
軟體環境
Ubuntu（VirtualBox）
PuTTY（Windows）
Buildroot
硬體環境
Raspberry Pi 3 Model B
USB to TTL 模組（CH340）
三、實驗步驟（Steps）
1️⃣ 序列埠登入 Raspberry Pi
(1) 接線方式
USB-TTL	Raspberry Pi
GND	GND
TX	RX
RX	TX

⚠️ 注意：TX 與 RX 必須交叉連接

(2) PuTTY 設定
Connection Type：Serial
Serial Line：COM9
Speed：115200
(3) 成功畫面
Welcome to Buildroot
buildroot login:

👉 表示成功透過 UART 登入系統

2️⃣ 擷取 image file 檔案
(1) 掛載 boot partition
sudo mount boot.vfat mnt1
驗證
ls mnt1

成功看到：

config.txt
start.elf
fixup.dat
bootcode.bin

👉 表示 boot partition 掛載成功

(2) 掛載 root filesystem
sudo mount rootfs.ext4 mnt2
驗證
ls mnt2

成功看到：

bin
etc
lib
usr

👉 表示 root filesystem 掛載成功

四、實驗結果（Result）

本實驗成功達成以下目標：

✅ 成功透過 UART 登入 Raspberry Pi
✅ 成功掛載 boot partition（boot.vfat）
✅ 成功掛載 root filesystem（rootfs.ext4）
✅ 不需燒錄 SD 卡即可存取 image file 內容
五、問題與解決（Difficulties & Solutions）
問題 1：USB 轉 TTL 無法使用
問題：原本使用 PL2303HXA，Windows driver 不支援
解決：更換為 CH340 USB 轉 TTL 模組
問題 2：PuTTY 無畫面
問題：接線錯誤（TX / RX 接反）
解決：
TX → RX
RX → TX
確保 GND 正確接線
問題 3：無法進入 output/images
問題：在 Raspberry Pi 環境中執行 cd 指令
解決：回到 Ubuntu（Buildroot）環境操作
問題 4：sdcard.img 無法 mount
問題：
offset 計算錯誤
sizelimit 計算錯誤
partition table 無法解析
解決：
改用：
boot.vfat
rootfs.ext4
直接掛載 filesystem

👉 成功擷取檔案

問題 5：losetup / kpartx 失敗
問題：無法建立 loopXp1 / loopXp2
解決：改用 filesystem 直接 mount
六、觀念整理（Concepts）
1️⃣ loop device 是什麼？

loop device 是一種虛擬裝置，可以將一個檔案（例如 image file）模擬成磁碟裝置，使其可以被 mount。

2️⃣ rsync 是什麼？

rsync 是一種檔案同步工具，可用於備份與資料複製。

rsync -axvH --delete source destination
特點：
只同步變動檔案（高效率）
保留權限與檔案結構
適合做增量備份
3️⃣ dd 與 rsync 差異
工具	特性
dd	bit-by-bit 複製整個磁碟（完整備份）
rsync	檔案層級同步（較有效率）
七、結論（Conclusion）

本實驗成功透過序列埠登入 Raspberry Pi，並利用 mount 指令從 image file 擷取 boot 與 root filesystem。

雖然 sdcard.img 無法在虛擬環境中正確解析 partition，但透過替代方式（boot.vfat 與 rootfs.ext4）仍成功達成實驗目標。

此外，也學習了：

loop device
rsync
dd

👉 對 Embedded Linux 系統有更深入的理解

八、補充說明

由於虛擬機環境無法直接存取 SD card，本實驗未進行 dd 與 rsync 實作，但已理解其原理與用途。
